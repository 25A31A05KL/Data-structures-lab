#include <iostream>
using namespace std;

int main()
{
    int q[100], item, f = -1, r = -1, i, size, choice, cont;

    cout << "\nEnter size of queue: ";
    cin >> size;

    do
    {
        cout << "\n1: Enqueue\n2: Dequeue\n3: Peek\n4: Display\n5: Exit";
        cout << "\nChoose any one option: ";
        cin >> choice;

        switch(choice)
        {
            case 1:
                if(r == size - 1)
                    cout << "\nQueue Overflow";
                else
                {
                    if(f == -1)
                        f = 0;
                    cout << "Enter item: ";
                    cin >> item;
                    r++;
                    q[r] = item;
                }
                break;

            case 2:
                if(f == -1 || f > r)
                    cout << "\nQueue Underflow";
                else
                {
                    cout << "\nDeleted item: " << q[f];
                    f++;
                    if(f > r) // reset queue
                        f = r = -1;
                }
                break;

            case 3:
                if(f == -1 || f > r)
                    cout << "\nQueue Underflow";
                else
                    cout << "\nFront element: " << q[f];
                break;

            case 4:
                if(f == -1 || f > r)
                    cout << "\nQueue Underflow";
                else
                {
                    cout << "\nQueue elements: ";
                    for(i = f; i <= r; i++)
                        cout << q[i] << " ";
                }
                break;

            case 5:
                exit(0);

            default:
                cout << "\nInvalid option";
        }

        cout << "\nEnter 1 to continue OR 0 to exit: ";
        cin >> cont;

    } while(cont);

    return 0;
}
